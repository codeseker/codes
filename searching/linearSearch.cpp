// linear search: Linear search is a Searching technique in which each element is compared with respect to any key
for (int i = 0; i < n; i++)
{
    if (arr[i] == key)
    {
        return "true" // i
    }
}
return false;